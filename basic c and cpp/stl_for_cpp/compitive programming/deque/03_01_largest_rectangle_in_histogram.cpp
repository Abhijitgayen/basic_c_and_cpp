#include<bits/stdc++.h>
using namespace std;
 int get_max_area( vector<int> a){
     int n= a.size(), ans=0;
     stack<int> st;
     for( int i=0; i<n;i++){

     }
 }
int main(){
    int n;
    cin>>n;
    vector<int> a(n);
    for( int i=0; i<n; i++){
        cin>>a[i];
    }
    cout<<get_max_area(a);
    return 0;
}